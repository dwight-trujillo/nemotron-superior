version: '3.9'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=production
  postgres:
    image: postgres:17.4-alpine
    environment:
      POSTGRES_PASSWORD: secret
  redis:
    image: redis:7.4.2-alpine
